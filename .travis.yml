language: python
python:
  - "2.7"

# Install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script:
    - cd include/test; pytest; rm -f test-blci/blci_deps.json

after_script:
    -  rm include/tests/test-blci/blci_deps.json
